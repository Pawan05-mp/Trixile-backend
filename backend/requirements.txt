# ── Web framework & server ──────────────────────────────────
fastapi[standard]==0.115.0
uvicorn[standard]==0.30.0
gunicorn>=22.0.0

# ── Database (async + sync drivers, migrations, GIS) ────────
sqlalchemy[asyncio]>=2.0,<3.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
geoalchemy2>=0.15.2

# ── Validation / settings ───────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.1.0
python-multipart>=0.0.9

# ── Auth / security ─────────────────────────────────────────
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.2

# ── HTTP client (social-auth token verification) ────────────
httpx>=0.27.0

# ── Data import / scripting ─────────────────────────────────
pandas>=2.2.2
openpyxl>=3.1.5

# ── Testing ─────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
